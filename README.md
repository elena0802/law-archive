# criminal-law-archive

## 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:3000`을 엽니다.

## 새 글 추가 방법

1. `content/essays` 폴더에 새 `.mdx` 파일을 만듭니다.
2. `content/essays/_template.mdx` 내용을 복사해 제목, 날짜, 분류를 바꿉니다.
3. 아래쪽에 본문을 씁니다.
4. 공개할 때 `draft: true`를 `draft: false`로 바꿉니다.
